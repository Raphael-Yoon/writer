# 빅데이터분석기사 8회 기출문제 (인쇄 원문)

---

## 1과목 빅데이터 분석 기획

### 01. 개인정보 처리단계 중 이용·제공 단계에 대한 설명으로 올바른 것은?
① 개인정보 수집 시 명확한 동의를 받아야 한다.
② 개인정보 파기 시 복구 불가능한 방법으로 파기해야 한다.
③ 개인정보를 목적 외로 이용하거나 제3자에게 제공해서는 안 된다.
④ 개인정보를 안전하게 관리하기 위해 기술적·관리적 보호조치를 취해야 한다.

### 02. 빅데이터의 3V에 해당하지 않는 것은?
① Volume (규모)
② Velocity (속도)
③ Variety (다양성)
④ Value (가치)

### 03. 데이터 비식별화 기술 중 가명처리(Pseudonymization)에 대한 설명으로 적절한 것은?
① 데이터의 일부 또는 전부를 삭제하여 식별성을 완전히 제거하는 기술이다.
② 개인을 식별할 수 있는 정보를 다른 값으로 대체하여 추가 정보 없이는 특정 개인을 알아볼 수 없도록 하는 기술이다.
③ 데이터의 범위를 넓혀 특정 개인을 특정할 수 없게 하는 기술이다.
④ 데이터에 잡음(Noise)을 추가하여 정확한 값을 알 수 없게 하는 기술이다.

### 04. 개인정보 보호법상 개인정보의 파기 방법으로 올바른 것은?
① 복구 및 재생이 불가능한 방법으로 파기해야 한다.
② 단순 삭제 후 복구 프로그램을 이용해 확인한다.
③ 휴지통에 버린 후 비운다.
④ 파일의 이름만 변경하여 저장한다.

---

### 05. 빅데이터 분석 기획 시 고려해야 할 사항으로 가장 적절하지 않은 것은?
① 분석의 목적과 목표를 명확히 정의한다.
② 필요한 데이터의 수집 가능성과 품질을 검토한다.
③ 분석 결과의 활용 방안을 사전에 수립한다.
④ 분석 기법과 알고리즘을 먼저 결정한 후 데이터를 수집한다.

### 06. 데이터 거버넌스(Data Governance)의 구성 요소로 올바르지 않은 것은?
① 데이터 표준화
② 데이터 관리 조직
③ 데이터 프로세스
④ 데이터 수집 도구 구매 비용

### 07. 하둡(Hadoop) 에코시스템 중 분산 코디네이터 역할을 담당하는 시스템은?
① Pig
② Hive
③ Zookeeper
④ HBase

### 08. 비струк처 데이터(Unstructured Data)의 예로 적절한 것은?
① 관계형 데이터베이스(RDBMS)의 테이블 데이터
② 텍스트, 이미지, 오디오, 비디오 파일
③ CSV 형식의 파일 데이터
④ Excel 시트 형태의 데이터

---

### 09. 개인정보 수집 및 이용에 관한 동의를 받을 때 고지해야 할 사항으로 옳지 않은 것은?
① 개인정보의 수집·이용 목적
② 수집하려는 개인정보의 항목
③ 개인정보의 보유 및 이용 기간
④ 개인정보 처리 위탁 업체의 재무 상태

### 10. 빅데이터 플랫폼의 구축 단계 중 가장 먼저 수행해야 하는 단계는?
① 시스템 아키텍처 설계
② 요구사항 분석 및 수립
③ 플랫폼 구축 및 테스트
④ 데이터 수집 및 연계

### 11. 데이터 품질 관리 기준 중 데이터가 실제 존재하는 객체와 정확히 일치하는지를 나타내는 지표는?
① 완전성(Completeness)
② 정확성(Accuracy)
③ 일관성(Consistency)
④ 유효성(Validity)

### 12. 분석 과제 발굴 방식 중 문제 정의(Problem Definition) 방식에 대한 설명으로 옳은 것은?
① 분석 대상 및 방법을 알고 있는 경우 수행하는 방식이다.
② 분석 대상을 모르고 분석 방법만 알고 있는 경우 수행하는 방식이다.
③ 분석 대상은 알고 있으나 분석 방법을 모르는 경우 수행하는 방식이다.
④ 분석 대상과 분석 방법 모두 모르는 경우 수행하는 방식이다.

### 13. 데이터 진단 단계에서 데이터 유형별 점검 사항으로 적절하지 않은 것은?
① 정형 데이터: 스키마 정합성, 속성값의 타당성
② 반정형 데이터: Tag 구조의 적절성, 파싱 오류 여부
③ 비정형 데이터: 텍스트 및 미디어 데이터의 손상 여부
④ 정형 데이터: 이미지 픽셀 해상도의 균일성

---

### 14. 마이데이터(MyData) 서비스의 개념으로 가장 적절한 것은?
① 정보주체가 자신의 개인정보를 주체적으로 관리하고 활용하는 서비스
② 기업이 고객의 데이터를 수집하여 타사에 판매하는 서비스
③ 정부가 국민의 개인정보를 일괄 통합하여 관리하는 시스템
④ 개인정보를 복원 불가능하도록 완전히 삭제해주는 서비스

### 15. 개인정보 안전성 확보조치 기준 중 기술적 보호조치에 해당하지 않은 것은?
① 접근 권한의 관리 및 통제
② 개인정보의 암호화
③ 내부관리계획의 수립 및 시행
④ 악성프로그램 방지 모듈 설치 및 운영

### 16. 분석 프로젝트의 위험 관리 대책 유형으로 옳지 않은 것은?
① 회피(Avoidance)
② 전가(Transference)
③ 완화(Mitigation)
④ 증대(Augmentation)

### 17. 기업의 분석 성숙도 모델(Analytic Maturity Model) 평가 대상 영역이 아닌 것은?
① 비즈니스 부문
② 조직 및 역량 부문
③ IT 인프라 부문
④ 대외 홍보 부문

### 18. 데이터 계생성(Data Lineage)에 대한 설명으로 가장 옳은 것은?
① 데이터의 생성부터 변경, 이동, 폐기까지의 흐름을 이력으로 관리하는 것
② 데이터의 암호화 키를 주주별로 분산 저장하는 기술
③ 데이터베이스의 인덱스를 자동 재구성하는 프로세스
④ 불필요한 빅데이터를 주기적으로 자동 삭제하는 규칙

---

### 19. 비즈니스 모델 캔버스(Business Model Canvas)의 구성 요소로 옳지 않은 것은?
① 핵심 파트너(Key Partners)
② 가치 제안(Value Propositions)
③ 데이터 전처리 알고리즘(Data Preprocessing Algorithm)
④ 고객 관계(Customer Relationships)

### 20. 분석 조직의 형태 중 각 사업부서에 분석 인력을 분산 배치하여 빠른 의무결정을 지원하는 형태는?
① 집중형 조직 구조
② 기능형 조직 구조
③ 분산형 조직 구조
④ 혼합형 조직 구조

---

## 2과목 빅데이터 탐색

### 21. 데이터를 정규화(Normalization)하는 주요 목적으로 가장 적절한 것은?
① 데이터의 전체 건수를 대폭 줄이기 위해
② 서로 다른 변수 간의 단위나 스케일 차이를 맞추기 위해
③ 범주형 변수를 연속형 변수로 변환하기 위해
④ 결측값을 자동으로 대치하기 위해

### 22. 박스 플롯(Box Plot)을 통해 직접 확인할 수 있는 지표가 아닌 것은?
① 중앙값(Median)
② 사분위수 범주(IQR)
③ 평균값(Mean)
④ 이상치(Outliers)

### 23. 표본 추출 방법 중 모집단을 서로 겹치지 않는 동질적인 여러 개의 층으로 나눈 후, 각 층에서 무작위로 표본을 추출하는 방법은?
① 단순 무작위 추출법(Simple Random Sampling)
② 계통 추출법(Systematic Sampling)
③ 층화 추출법(Stratified Random Sampling)
④ 군집 추출법(Cluster Sampling)

### 24. 탐색적 데이터 분석(EDA)의 주요 활동으로 적절하지 않은 것은?
① 변수별 분포 및 통계량 확인
② 변수 간 상관관계 분석 및 시각화
③ 가설 검정을 위한 모형 선택 및 예측 수행
④ 결측값 및 이상치의 탐색

---

### 25. 주성분 분석(PCA)에 대한 설명으로 옳지 않은 것은?
① 고차원 데이터를 저차원으로 축소하는 비지도 학습 기법이다.
② 기존 변수들의 선형 결합으로 새로운 주성분들을 생성한다.
③ 첫 번째 주성분(PC1)은 데이터의 분산을 가장 적게 설명하는 축이다.
④ 주성분 상호 간에는 서로 직교(Orthogonal)하며 상관관계가 0이다.

### 26. 데이터 전처리 기법 중 스케일링 방법인 Min-Max Scaling의 수식으로 옳은 것은?
① $X' = \frac{X - Mean}{StdDev}$
② $X' = \frac{X - Min}{Max - Min}$
③ $X' = \log(X)$
④ $X' = \frac{X}{Max}$

### 27. 요인 분석(Factor Analysis)과 주성분 분석(PCA)의 비교 설명으로 옳지 않은 것은?
① 요인 분석은 잠재된 요인을 추출하는 것이 목적이다.
② PCA는 데이터의 정보 손실을 최소화하면서 차원을 축소하는 것이 목적이다.
③ 요인 분석의 요인들은 서로 독립성을 가지지 않을 수도 있다.
④ PCA에서는 변수들의 선형 결합으로 원본 데이터 전체를 온전히 설명해야 하므로 관측 오차를 별도로 고려하지 않는다.

### 28. 연속형 변수의 이상치 탐지 방법으로 적절하지 않은 것은?
① Z-Score 방법
② IQR (Interquartile Range) 방법
③ Mahalanobis Distance 방법
④ 빈도수 분석 (Frequency Analysis)

---

### 29. 연속형 변수를 범주형 변수로 변환하는 데이터 가공 기법은?
① 이산화(Discretization)
② 정규화(Normalization)
③ 부호화(Encoding)
④ 중심화(Centering)

### 30. 표본평균의 표집분포에 대한 설명으로 옳은 것은?
① 모집단의 분포와 상관없이 표본의 크기 n이 커지면 표본평균의 분포는 정규분포에 가까워진다.
② 표본평균의 분산은 모집단의 분산보다 항상 크다.
③ 표본평균의 기대치는 모집단의 평균과 항상 다르다.
④ 모집단이 정규분포가 아니면 표본의 크기와 상관없이 정규분포를 따르지 않는다.

### 31. 데이터 편중에 관한 지표 중 왜도(Skewness)에 대한 설명으로 옳은 것은?
① 왜도가 0보다 크면 오른쪽으로 긴 꼬리를 가진다.
② 왜도가 0이면 왼쪽으로 긴 꼬리를 가진다.
③ 왜도가 0보다 작으면 중앙값보다 평균이 더 크다.
④ 왜도는 분포의 뾰족한 정도를 나타낸다.

### 32. 귀무가설($H_0$)과 대립가설($H_1$)에 대한 설명으로 올바른 것은?
① 귀무가설은 기존의 주장이나 효과가 없다는 가설이다.
② p-value가 유의수준 $\alpha$보다 크면 귀무가설을 기각한다.
③ 1종 오류는 귀무가설이 거짓일 때 귀무가설을 채택하는 오류이다.
④ 유의수준 $\alpha$는 2종 오류를 범할 확률의 최댓값이다.

### 33. 베르누이 시행(Bernoulli trial)에 대한 설명으로 옳지 않은 것은?
① 매 시행마다 결과는 오직 두 가지(성공 또는 실패)로 나타난다.
② 각 시행은 서로 독립이다.
③ 매 시행마다 성공 확률 $p$는 변한다.
④ 성공 확률이 $p$일 때 실패 확률은 $1-p$이다.

---

### 34. 편향-분산 트레이드오프(Bias-Variance Tradeoff)에 대한 설명으로 옳지 않은 것은?
① 모델의 복잡도가 증가하면 편향은 감소하고 분산은 증가한다.
② 편향이 높으면 과소적합(Underfitting)이 발생할 수 있다.
③ 분산이 높으면 과적합(Overfitting)이 발생할 수 있다.
④ 편향과 분산 모두를 동시에 0으로 만드는 모델을 쉽게 구축할 수 있다.

### 35. Box-Cox 변환에 대한 설명으로 옳지 않은 것은?
① 정규성 조건을 만족하지 않는 데이터를 정규분포에 가깝게 변환하는 방법이다.
② 파라미터 $\lambda$의 값에 따라 다양한 변환 함수가 적용된다.
③ 음수 데이터를 포함한 모든 데이터에 바로 적용할 수 있다.
④ $\lambda=0$일 때 로그 변환($\ln X$)이 적용된다.

### 36. 표본 크기와 추정량의 성질에 대한 설명으로 옳은 것은?
① 표본의 크기가 커지면 표본평균의 표준오차는 커진다.
② 표본평균은 모집단 평균의 불편추정량(Unbiased Estimator)이다.
③ 표본의 크기가 작을수록 일치성(Consistency)이 증가한다.
④ 효율성(Efficiency)은 추정량의 분산이 클수록 높아진다.

### 37. 차원 축소(Dimensionality Reduction) 기법에 해당하지 않는 것은?
① PCA (주성분 분석)
② t-SNE
③ LDA (선형 판별 분석)
④ k-Means 클러스터링

### 38. 편상관계수(Partial Correlation Coefficient)에 대한 설명으로 옳은 것은?
① 두 변수 간의 관계를 측정할 때 제3의 변수의 영향을 통제한 후 구한 상관계수이다.
② 항상 단순 상관계수보다 값이 크다.
③ 범주형 변수 간의 연관성을 측정할 때 사용한다.
④ 제3의 변수를 추가할수록 항상 값이 증가한다.

---

### 39. 다변량 분산분석(MANOVA)에 대한 설명으로 옳은 것은?
① 종속변수가 2개 이상일 때 집단 간 차이를 검정하는 방법이다.
② 독립변수가 반드시 연속형 변수이어야 한다.
③ 단일 종속변수에 대해 여러 독립변수의 영향을 분석한다.
④ 비모수 검정 기법 중 하나이다.

### 40. 결측값 대치법 중 MCMC(Markov Chain Monte Carlo)를 사용하는 방법은?
① 단순 대치법
② 평균 대치법
③ 다중 대치법 (Multiple Imputation)
④ 조건부 평균 대치법

### 41. 다중공선성(Multicollinearity)을 진단하는 지표로 사용되는 VIF(Variance Inflation Factor)에 대한 설명으로 옳은 것은?
① VIF가 10 이상이면 다중공선성이 심각하다고 판단한다.
② VIF가 0에 가까울수록 다중공선성이 높다.
③ VIF는 독립변수와 종속변수 간의 상관계수 제곱이다.
④ VIF는 다중공선성이 없을 때 $\infty$의 값을 가진다.

### 42. 표본 추출 시 모집단을 특정 기준에 따라 나눈 후 각 집단에서 임의 추출하는 확률 표본 추출법은?
① 층화 표본 추출법
② 편의 표본 추출법
③ 스노우볼 표본 추출법
④ 판단 표본 추출법

---

## 3과목 빅데이터 모델링

### 43. Seq2Seq 모델에서 Encoder가 Input 문장을 압축하여 Decoder로 전달하는 벡터는?
① Context Vector (컨텍스트 벡터)
② One-hot Vector
③ Embedding Vector
④ Latent Vector

### 44. 경사하강법(Gradient Descent)에서 학습률(Learning Rate)이 너무 클 때 발생하는 문제는?
① 기울기가 0이 되어 학습이 정지된다.
② 최적점에 수렴하지 못하고 발산할 수 있다.
③ 학습 속도가 너무 느려진다.
④ 과적합(Overfitting)이 항상 방지된다.

### 45. 머신러닝에서 하이퍼파라미터(Hyperparameter)에 해당하는 것은?
① 선형 회귀의 회귀 계수(Weight)
② 의사결정나무의 분할 기준 파라미터
③ 신경망의 가중치 및 편향
④ 딥러닝 학습 시의 학습률(Learning Rate) 및 에포크(Epoch) 수

### 46. 서포트 벡터 머신(SVM)에서 마진(Margin)을 최대화하는 경계면을 형성하는 데이터 포인트들을 무엇이라 하는가?
① Support Vectors (서포트 벡터)
② Decision Boundaries
③ Kernel Vectors
④ Centroids

---

### 47. 민코프스키 거리(Minkowski Distance) 공식에서 $p=1$일 때 해당하는 거리는?
① 유클리드 거리 (Euclidean Distance)
② 맨해튼 거리 (Manhattan Distance)
③ 체비쇼프 거리 (Chebyshev Distance)
④ 마하노비스 거리 (Mahalanobis Distance)

### 48. 의사결정나무(Decision Tree)의 분할 기준 지표로 사용되지 않는 것은?
① 지니 계수 (Gini Index)
② 엔트로피 (Entropy)
③ 정보 획득량 (Information Gain)
④ 결정계수 ($R^2$)

### 49. 부스팅(Boosting) 앙상블 알고리즘에 해당하지 않는 것은?
① AdaBoost
② Gradient Boosting Machine (GBM)
③ XGBoost
④ Random Forest

### 50. 인공신경망의 활성화 함수(Activation Function) 중 입력값이 0 이하일 때는 0, 0 초과일 때는 입력값 그대로를 출력하는 함수는?
① Sigmoid
② ReLU
③ Tanh
④ Softmax

---

### 51. 앙상블 기법 중 배깅(Bagging)에 대한 설명으로 옳지 않은 것은?
① Bootstrap Aggregating의 약자이다.
② 복원 추출을 통해 여러 개의 보트스트랩 샘플을 생성한다.
③ 이전 모델의 오차를 보완하는 방식으로 순차적으로 학습한다.
④ Random Forest는 배깅을 기반으로 한 대표적인 알고리즘이다.

### 52. 오즈비(Odds Ratio)에 대한 설명으로 옳은 것은?
① 두 집단 간의 성공 확률의 차이를 의미한다.
② 성공 확률을 실패 확률로 나눈 비율의 비이다.
③ 항상 0과 1 사이의 값을 가진다.
④ 상대위험도(Relative Risk)와 완전히 동일한 지표이다.

### 53. 나이브 베이즈(Naive Bayes) 분류기에서 '나이브(Naive)'의 의미로 가장 적절한 것은?
① 모든 특성(Feature)들이 서로 독립이라고 가정하는 것
② 계산 과정이 매우 단순하다는 것
③ 하이퍼파라미터가 존재하지 않는다는 것
④ 딥러닝 구조를 사용하지 않는다는 것

### 54. 모수적 검정과 비모수적 검정에 대한 설명으로 옳지 않은 것은?
① 모수적 검정은 모집단의 분포에 대한 가정이 필요하다.
② 비모수적 검정은 표본의 수가 적거나 정규성 가정을 만족하지 못할 때 사용한다.
③ 비모수적 검정에서는 순위(Rank) 데이터를 이용하는 경우가 많다.
④ Wilcoxon 부호순위 검정은 대표적인 모수적 검정 방법이다.

---

### 55. 결정계수($R^2$)에 대한 설명으로 옳지 않은 것은?
① 회귀 모델의 설명력을 나타내는 지표이다.
② 값의 범위는 0에서 1 사이이다.
③ 독립변수의 개수가 증가하면 항상 감소한다.
④ $R^2 = 1 - \frac{SSE}{SST}$ 로 계산된다.

### 56. 미니배치 경사하강법(Mini-batch Gradient Descent)의 특징으로 옳은 것은?
① 전체 데이터를 한 번에 학습하여 가중치를 업데이트한다.
② 단 하나의 샘플만을 이용하여 가중치를 업데이트한다.
③ 전체 데이터를 일정 크기로 나누어 배치를 구성한 뒤 가중치를 업데이트한다.
④ 학습 속도가 가장 느리지만 안정적이다.

### 57. 릿지 회귀(Ridge Regression)에 대한 설명으로 옳은 것은?
① L1 규제(Penalty)를 사용하는 회귀 모델이다.
② 가중치들의 절대값의 합을 규제항으로 추가한다.
③ L2 규제를 사용하며, 회귀 계수를 0으로 완전히 수축시키지는 않는다.
④ 중요하지 않은 변수의 회귀 계수를 0으로 만들어 변수 선택 효과가 있다.

### 58. 머신러닝 모델의 과적합(Overfitting)을 방지하기 위한 방법으로 적절하지 않은 것은?
① 데이터의 양을 늘린다.
② 규제(Regularization) 기법을 적용한다.
③ 모델의 복잡도(파라미터 수)를 높인다.
④ 드롭아웃(Dropout)을 적용한다.

### 59. 선형 회귀와 로지스틱 회귀의 차이점에 대한 설명으로 옳지 않은 것은?
① 선형 회귀의 종속변수는 연속형이다.
② 로지스틱 회귀의 종속변수는 범주형이다.
③ 로지스틱 회귀는 시그모이드(Sigmoid) 함수를 사용한다.
④ 선형 회귀는 분류 문제에, 로지스틱 회귀는 회귀 문제에 주로 사용된다.

---

### 60. 모델의 배치에 관한 설명으로 옳지 않은 것은?
① 배치 크기가 작으면 훈련속도가 빨라진다.
② 배치 크기는 훈련속도에 영향을 주지만 성능에 영향을 없다.
③ 배치 크기가 너무 크면 메모리 문제가 발생한다.
④ 배치 크기가 너무 작으면 노이즈가 생기며 모델의 학습에 악영향을 준다.

---

## 4과목 빅데이터 결과 해석

### 61. 불균형 데이터에 대한 설명으로 옳지 않은 것은?
① 데이터 불균형이 있는 경우 최적화된 모델의 학습이 어려울 수 있다.
② 불균형 데이터 집합에서는 정확도보다는 정밀도를 평가지표로 설정해야 한다.
③ 학습 시 클래스의 개수보다는 클래스 간의 샘플 수 차이에 영향을 받는다.
④ 소수의 클래사는 언더샘플링을 적용해 해결한다.

### 62. 결측값을 대치하는 방법 중 회귀대치법에 대한 설명으로 맞지 않은 것은?
① 대체할 결측값을 예측하기 위해 회귀분석을 사용한다.
② 데이터의 구조와 패턴을 반영하여 결측값을 대체할 수 있다.
③ 독립변수와 종속변수 간의 관계가 약할 경우에도 적용이 가능하다.
④ 결측값이 없는 다른 변수를 이용하여 결측값이 있는 변수를 예측한다.

### 63. 시계열 자료에서 예측 정확도를 측정하는 지표에 대한 설명으로 적절하지 않은 것은?
$d_t$ : 시간 t에서의 실제값, $\hat{d}_t$ : 시간 t에서의 예측값, $e_t = d_t - \hat{d}_t$, $n$ : 시계열 자료의 개수

① $MAE = \frac{1}{n} \sum_{t=1}^n |e_t|$ 로 표현되고, 실제값과 예측값 차이의 절대값을 평균한 것이다.
② $MSE = \frac{1}{n} \sum_{t=1}^n e_t^2$ 로 표현되고, 실제값과 예측값 차이의 제곱합을 평균한 것이다.
③ $MAPE = \frac{1}{n} \sum_{t=1}^n \left|\frac{e_t}{d_t}\right|$ 로 표현되고, 실제값과 예측값 비의 절대치를 평균한 것이다.
④ $MPE = \frac{100\%}{n} \sum_{t=1}^n \frac{e_t}{d_t}$ 로 표현되고, 상대적 예측 오차를 계산하는데 사용된다.

---

### 64. ROC 곡선에 대한 설명으로 옳지 않은 것은?
① FPR 값에 따른 TPR 값의 그래프이다.
② FPR이 작아도 TPR이 클 수 있다.
③ 무작위의 경우 TPR과 FPR은 같은 곳으로 수렴한다.
④ AUC 값이 작을수록 좋은 모델이다.

### 65. 척도와 예시가 맞지 않게 연결된 것은?
① 비율 척도 - 나이
② 명목 척도 - 성별
③ 서열 척도 - 매출액
④ 등간 척도 - 온도

### 66. 실제 Positive인 대상 중에서 Positive로 정확히 예측한 확률을 뜻하는 것은?
① 재현율(Recall)
② 정확도(Accuracy)
③ 정밀도(Precision)
④ 특이도(Specificity)

### 67. 주어진 혼동행렬을 활용하여 평가지표를 계산한 결과로 적절하지 않은 것은?

| 실제 값 \ 예측 결과 | Positive | Negative |
| :--- | :--- | :--- |
| Positive | 48 | 12 |
| Negative | 2 | 38 |

① 정확도 0.86
② 민감도 0.75
③ 특이도 0.95
④ 정밀도 0.96

---

### 68. 아래 빈칸에 들어갈 내용으로 잘못된 것은?

| 요인 | 제곱합 | 자유도 | 평균제곱 | F 값 |
| :--- | :--- | :--- | :--- | :--- |
| 회귀 | 18.667 | 2 | (3) | (4) |
| 잔차 | 2.78 | (2) | 0.31 | |
| 합계 | (1) | 11 | | |

① (1) - 21.447
② (2) - 9
③ (3) - 6.222
④ (4) - 30.11

### 69. 바이너리(binary) 변수에 대한 설명으로 맞지 않은 것은?
① 두 가지 값만 가질 수 있는 변수이다.
② 성별(남, 여), 출석상태(출석, 미출석) 등이 바이너리 변수이다.
③ 로지스틱 회귀와 같은 분류 모델에서 사용된다.
④ 원-핫 인코딩은 연속형 데이터를 이진 형식으로 변환한다.

### 70. k-fold 교차검증에 대한 설명으로 옳지 않은 것은?
① k-1개 데이터셋은 학습용으로 사용하고, 1개 데이터셋은 검증용으로 사용한다.
② 폴드(fold)의 크기가 작을수록 모델의 성능이 떨어진다.
③ 학습과 검증을 k번 반복해서 수행한다.
④ k개로 나누어진 데이터셋은 각각 한 번씩만 검증용으로 사용된다.

### 71. (가)와 (나)를 표현하기에 적합한 인포그래픽으로 가장 잘 연결된 것은?
> (가) 지역별 코로나 발생률
> (나) 코로나 발생 이후부터 월별 코로나 발생률

① (가) 지도 인포그래픽, (나) 타임라인 인포그래픽
② (가) 목록 인포그래픽, (나) 타임라인 인포그래픽
③ (가) 지도 인포그래픽, (나) 프로세스 인포그래픽
④ (가) 비교 인포그래픽, (나) 통계 인포그래픽

---

### 72. 다음 그래프는 1998년부터 2020년까지의 출생자수와 주택매매가의 변동을 보여준다. 그래프에 대한 해석으로 잘못된 것은?
① 2000년 출생자는 600,000명을 초과했다.
② 2003년 주택매매가는 2004년보다 낮다.
③ 주택매매가는 매년 상승하고 있다.
④ 출생자수와 주택매매가는 음의 상관관계가 있다.

### 73. 다음 중 교차검증에 대한 설명으로 옳지 않은 것은?
① 시계열 데이터에서 학습데이터와 검증데이터는 같은 시간대에 있어야 한다.
② 학습 데이터에서의 평균제곱오차 값은 대개 검증 데이터에서의 평균제곱오차 값보다 작다.
③ k-폴드 교차검증은 k번의 학습과 검증을 진행한다.
④ 교차검증은 모델의 훈련시간이 증가한다.

---

### 74. 다음은 OECD 국가 중 유럽과 그 외 국가의 GDP에 대한 박스 플롯이다. 해석으로 옳지 않은 것을 고르시오.
① 유럽 국가 GDP의 중앙값은 비유럽 국가 GDP의 중앙값보다 크다.
② 유럽 국가 GDP의 평균이 비유럽 국가 GDP의 평균보다 신뢰구간 95%에서 유의하게 높다.
③ 비유럽 국가 중에서 GDP가 가장 높은 국가의 GDP는 10000이다.
④ 유럽 국가의 IQR은 2000이다.

### 75. 국회의원 선거에서 지역 면적이 아니라 지역구에 당선된 국회의원 수에 따라 시각화 할 때 적합한 시각화 도구는?
① 카토그램
② 단계구분도
③ 픽토그램
④ 하이퍼볼릭 트리

### 76. 모자이크 플롯에 대한 설명으로 맞지 않은 것은?
① 변수에 속한 값의 분포를 시각적으로 표현한다.
② 두 개 이상의 범주형 데이터의 상관관계를 나타낸다.
③ 열의 너비는 가로 축에 표시된 관측치 수에 비례한다.
④ 히스토그램 안에 히스토그램이 있는 형식이다.

### 77. 다음 중 기초통계량과 그래프로 확인할 수 없는 것을 고르시오.
① 결측치
② 이상치
③ 통계적 유의성
④ 데이터 분포

---

### 78. 데이터 시각화의 순서로 옳은 것은?
① 데이터 획득 $\rightarrow$ 데이터 구조화 $\rightarrow$ 데이터 마이닝 $\rightarrow$ 시각화모델 선택 $\rightarrow$ 시각화 표현
② 데이터 획득 $\rightarrow$ 데이터 구조화 $\rightarrow$ 시각화모델 선택 $\rightarrow$ 시각화 표현 $\rightarrow$ 데이터 마이닝
③ 데이터 구조화 $\rightarrow$ 데이터 획득 $\rightarrow$ 시각화모델 선택 $\rightarrow$ 데이터 마이닝 $\rightarrow$ 시각화 표현
④ 데이터 구조화 $\rightarrow$ 데이터 획득 $\rightarrow$ 데이터 마이닝 $\rightarrow$ 시각화모델 선택 $\rightarrow$ 시각화 표현

### 79. 지역별 매출과 수익을 시각화 하기에 가장 적절한 방법으로 짝지어진 것은?
① 매출: 버블차트, 수익: 코로플레스맵
② 매출: 코로플레스맵, 수익: 버블차트
③ 매출: 카토그램, 수익: 버블차트
④ 매출: 등치선도, 수익: 카토그램

### 80. 분석 결과 활용 계획에 대한 설명으로 옳지 않은 것은?
① 내·외부 교육 훈련 방안도 포함한다.
② 분석 결과 활용 계획은 분석 모형 리모델링 후 수립한다.
③ 분석 결과 활용 효과 측정을 위한 성과지표 마련되어야 한다.
④ 분석 결과에 대한 지속적인 모니터링이 필요하다.
